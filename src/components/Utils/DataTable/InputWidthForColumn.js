const extraSmallWidth = { width: '6.5em' }

const smallWidth = { width: '8em' }

const mediumWidth = { width: '10em' }

const largeWidth = { width: '20em' }

const bodyStyle = { textAlign: 'center' }

const headerStyle = { width: '10%', minWidth: '8rem' }

export { extraSmallWidth, smallWidth, mediumWidth, largeWidth, bodyStyle, headerStyle };
